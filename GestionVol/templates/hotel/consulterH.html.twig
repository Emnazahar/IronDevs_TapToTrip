{% extends 'base-back.html.twig' %}
{% block body %}
    <a href="{{ path('mails') }}">Nous Contacter </a>

   {# <div class="jumbotron">
        <div class="container">
            {{ form_start(filtre) }}
            <div class="form-row">
                <div class="col">
                    {{ form_row(filtre.maxetoiles) }}
                </div>
                <div class="form-row">
                    {{ form_row(filtre.adresseS) }}
                </div>
                <div class="col">
                    <button class="btn btn-primary">Filtrer</button>
                </div>

        </div>
            {{ form_end(filtre) }}
        </div>
        </div>#}
    <title> TapToTrip | Liste des Hotels  </title>
    <link rel="stylesheet" href="https://fonts.googleapis.com/css?family=Roboto|Varela+Round">
    <link rel="stylesheet" href="https://fonts.googleapis.com/icon?family=Material+Icons">
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/font-awesome/4.7.0/css/font-awesome.min.css">
    <script src="https://code.jquery.com/jquery-3.5.1.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/popper.js@1.16.0/dist/umd/popper.min.js"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.0/js/bootstrap.min.js"></script>

    <!-- css -->
    <link rel="stylesheet" href="{{ asset ('Back/css/styleList.css') }}">


    <div class="container-xl">
        <div class="table-responsive">
            <div class="table-wrapper">
                <div class="table-title">
                    <div class="row">
                        <div class="col-sm-6">

                            {#  <h2>Liste des <b>Hotels</b></h2>
                            <form method="post" action="{{ path( 'rechercheH' ) }}">
                                <label>Recherche</label>
                                <input type="text" name="search"> <input type="submit" value="Recherche">
                            </form>#}
                        </div>
                        <div class="col-sm-6">
  <div class="btn btn-success"> <i class="material-icons">&#xE147;</i><span></span><a href="{{ path('addH') }}">Nouvel Hotel</a>
                        </div>
                    </div>
                </div>

                <!-- Tables des attractions -->
                <table class="table table-striped table-hover">
                    <thead>
                    <tr>
                        <th><b>ID</b></th>
                        <th><b>Nom Hotel</b></th>
                        <th><b>Nombre Des Etoiles</b></th>
                        <th><b>Adresse</b></th>
                        <th><b>Description</b></th>
                        <th><b>Chambres</b></th>
                        <th><b>Modifier/Supprimer</b></th>

                    </tr>
                    </thead>

                    <tbody>
                    </tr>
                    {% for h in hotel %}
                        <tr>
                            <td>{{ h.id }}</td>
                            <td>{{ h.nomH }}</td>
                            <td>{{ h.nbEtoiles }}</td>
                            <td>{{ h.adresse }}</td>
                            <td>{{ h.descriptionH}}</td>

                            <td>
                                {% if h.chambre is not null %}
                            {{ h.chambre.id }}
                                {% else %}
                                    N/A
                                {% endif %}
                                |
                                {% if h.chambre is not null %}
                                    {{ h.chambre.nbrlits }}
                                {% else %}
                                    N/A
                                {% endif %}
                                |
                                {% if h.chambre is not null %}
                                    {{ h.chambre.typeCh }}
                                {% else %}
                                    N/A
                                {% endif %}
                                |
                                {% if h.chambre is not null %}
                                    {{ h.chambre.prix }}
                                {% else %}
                                    N/A
                                {% endif %}

                            </td>

                            {# <td><img src="{{ i.image }}"></td>#}
                            <td>
                                <a href="{{ path('update',{'id': h.id}) }}" class="edit" ><i class="material-icons" data-toggle="tooltip" title="Edit">&#xE254;</i></a>
                                <a href="{{ path('delete',{'id': h.id}) }}" class="delete" ><i class="material-icons" data-toggle="tooltip" title="Delete">&#xE872;</i></a>

                            </td>
                        </tr>

                    {% endfor %}

                    </tbody>
                </table>
                <a href="{{('pdf') }}">Imprimer <b>PDF</b></a>
                <div class="navigation">
                    {{ knp_pagination_render(hotel) }}

                    <div class="btn btn-success"> <i class="material-icons">&#xE147;</i><span></span><a href="{{ path('hotel') }}">Retour vers <b>L'Acceuil</b></a></div>
                    <div class="btn btn-success"> <i class="material-icons">&#xE147;</i><span></span><a href="{{ path('chambre') }}">Consulter le <b>Front</b></a>
                </div>

            </div>
        </div>
    </div>
    </div>
    </div>
{% endblock %}
